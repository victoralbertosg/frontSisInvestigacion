import { LocationChangeEvent } from '@angular/common';

export interface Usuario{
    idusuario: number;
    idpersona: number;
    idespecialidad:number;
    rol:number;
    password:string;
    nivel:number;
}